# Display-first-character-at-index-0
This simple Java program demonstrates how to access and display the first character of a string. The program uses the Scanner class for potential user input (though here a fixed string is used), and the charAt(0) method to retrieve the character at index 0.
